# TODO List

## UI & Frontend
- [ ] Add frontend UI for managing instructions (Global, FSDM, and Mapping scopes) stored in the database.

## Agents & Tools
- [ ] Update `lg_get_instructions` in `agent/tools/tools.py` to fetch actual content from the project database once the UI is implemented.
